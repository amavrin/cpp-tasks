#include "Header.h"

// sum_some() accumulates and return the sum of v's elements whose value are LT (less than), GT (greater than) or EQ (equals to)
// the value, passed in the parameter 'x
// LT, GT or EQ passed in the 'op' parameter
int sum_some(vector<int> v, int op, int x)
{
	return 0;
}